<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Nền màu (có thể chỉnh lại cho phù hợp theme) -->
    <solid android:color="#FFFFFF" />

    <!-- Bo tròn góc -->
    <corners android:radius="10dp" />

    <!-- Padding bên trong (nếu dùng trực tiếp làm background EditText) -->
    <padding
        android:left="10dp"
        android:top="10dp"
        android:right="10dp"
        android:bottom="10dp" />
</shape>
